name: ci

on:
  push:
    branches:
      - main
      - master
  pull_request:

# Dwa joby:
# - test: szybki feedback (build + unit testy). Musi przejść żeby PR mógł być zmergowany.
# - smoke: wolniejszy, testuje czy zbudowana apka w ogóle startuje (predeploy). Można
#   pominąć przez label `skip-smoke` na PR.
jobs:
  test:
    name: Build & test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          # Jedno źródło prawdy dla wersji Node: .nvmrc (obecnie 24.12.0),
          # spójne z package.json engines.node (24.x). Nie hardkodować wersji tutaj.
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        # Lint jest teraz warnings-only (eslint --max-warnings 0 by failowało
        # na istniejących 35 warningach). Gdy liczba spadnie, zmień na
        # `npm run lint -- --max-warnings 0` żeby failować na nowych.
        run: npm run lint

      - name: Lint warning budget (nieblokujący)
        # Krok diagnostyczny: raportuje liczbę warningów, ale NIE failuje builda.
        # Cel: widoczność dryfu jakości w czasie. Gdy budżet osiągnie 0, usuń ten
        # krok i dodaj `--max-warnings 0` do kroku "Lint" powyżej.
        # BUDŻET (do obniżania, nigdy do podnoszenia): 35
        continue-on-error: true
        run: |
          echo "Aktualny budżet ostrzeżeń: 35 (cel: 0)"
          npm run lint -- --max-warnings 0 || echo "::warning::Lint przekracza docelowy budżet 0 ostrzeżeń. Obniżaj, nie podnoś."

      - name: Build
        run: npm run build

      - name: Unit tests
        run: npm run test

  smoke:
    name: Pre-deploy smoke
    runs-on: ubuntu-latest
    needs: test
    # Smoke startuję zbudowaną apkę i sprawdza czy kluczowe routy odpowiadają.
    # Wymaga działającego serwera (predeploy), więc jest wolniejszy i bardziej
    # wrażliwy na network. Domyślnie włączony; do pominięcia na PR dodaj label
    # `skip-smoke` (GitHub: gh pr edit --add-label skip-smoke).
    if: ${{ !contains(github.event.pull_request.labels.*.name, 'skip-smoke') }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Run smoke against local build
        run: npm run smoke:online
        env:
          DINBOARD_SMOKE_PORT: "4173"
